# Copy to .env locally. On Render, set these in Dashboard → Environment.

PORT=5000
NODE_ENV=development

MONGO_URI=mongodb+srv://ayushneupane2022_db_user:ayush123@cluster0.wehdi5d.mongodb.net/PlayPal


JWT_SECRET_KEY=your-long-random-secret

FRONTEND_URL=http://localhost:5173

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUD_NAME=
CLOUD_API=
CLOUD_API_SECRET=

# Verified sender in Brevo → Senders (e.g. playpal602@gmail.com)
BREVO_SENDER_EMAIL=
# API key from Brevo → SMTP & API → API Keys (xkeysib-...) — required on Render
BREVO_API_KEY=
# SMTP key (xsmtpsib-...) — optional, for local dev only
BREVO_SMTP_KEY=
# Optional: SMTP login if different from sender (Brevo account email)
# BREVO_SMTP_LOGIN=
ADMIN_EMAIL=

KHALTI_SECRET_KEY=
KHALTI_BASE_URL=https://a.khalti.com/api/v2
